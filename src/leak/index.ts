import { json2csv } from 'json-2-csv';
// import leakedData from '../../leak.json';
import { writeFile } from 'fs/promises';

export async function transformLeak() {
  // const csv = await json2csv(leakedData);
  // await writeFile('./leak.csv', csv);
}
